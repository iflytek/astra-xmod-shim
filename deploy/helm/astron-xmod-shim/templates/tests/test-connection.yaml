apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "astron-xmod-shim.fullname" . }}-test-connection"
  labels:
    {{- include "astron-xmod-shim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "astron-xmod-shim.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
