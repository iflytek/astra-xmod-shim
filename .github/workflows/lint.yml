name: Lint & Format

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true

      - name: Clean old golangci-lint
        run: |
          rm -f ./bin/golangci-lint
          mkdir -p ./bin


      - name: Install golangci-lint (latest master for Go 1.24)
        run: |
          # 从 master 分支安装最新版本（支持 Go 1.24）
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b ./bin master
      - name: Verify golangci-lint version
        run: |
          ./bin/golangci-lint --version

      - name: Run golangci-lint
        run: ./bin/golangci-lint run --timeout=5m